import { MaiorDeIdade } from './maior-de-idade';

describe('MaiorDeIdade', () => {
  it('should create an instance', () => {
    const directive = new MaiorDeIdade();
    expect(directive).toBeTruthy();
  });
});
